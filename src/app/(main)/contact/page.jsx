import ContactHeader from '@/components/Contact/ContactHeader';
import React from 'react';

const page = () => {
    return (
        <div>
            <ContactHeader/>
        </div>
    );
};

export default page;