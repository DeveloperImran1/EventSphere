import React from 'react';

const page = () => {
    return (
        <div>
        This is admin profile page . Another page is cannot add in this time.
        
        </div>
    );
};

export default page;